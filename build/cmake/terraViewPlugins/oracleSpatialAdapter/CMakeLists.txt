


SET( CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE ${CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE}/plugins)
SET( CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG ${CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG}/plugins)  

set (ORACLESPATIALADAPTER_TVPLGS_DIR "${TVPLGS_DIR}/oracleSpatialAdapter") # change this if terralib is somewhere else
set ( PROJ_NAME "oracleSpatialAdapter" )

project ( ${PROJ_NAME} )

if( (NOT DEFINED TE_DIRS) OR  (NOT DEFINED TE_LIBS) ) 
	find_package ( terralib REQUIRED )
endif()

include_directories (
	${TE_DIRS}
	${TV_DIRS}
	${ORACLESPATIALADAPTER_TVPLGS_DIR}/include
	${QT_INCLUDE_DIRS}
	"${CMAKE_CURRENT_BINARY_DIR}"
)

FILE ( GLOB SRCS ${ORACLESPATIALADAPTER_TVPLGS_DIR}/src/*.cpp )
FILE ( GLOB HEADERS ${ORACLESPATIALADAPTER_TVPLGS_DIR}/include/*.h )

#Qt files
#Requires Qt3 multi-threaded
set(QT_MT_REQUIRED, TRUE)
FIND_PACKAGE(Qt3 REQUIRED)

set (
	TO_MOC
	${ORACLESPATIALADAPTER_TVPLGS_DIR}/include/oraclespatialadapterPlugin.h
)

file( GLOB UI_FILES ${ORACLESPATIALADAPTER_TVPLGS_DIR}/ui/*.ui )
file( GLOB H_UI_FILES ${ORACLESPATIALADAPTER_TVPLGS_DIR}/ui/*.h )
QT_WRAP_UI( ${PROJ_NAME} HDRS uiFiles ${UI_FILES} )
QT_WRAP_CPP ( ${PROJ_NAME} moc_files ${TO_MOC} )


source_group( "Generated Files\\Header" FILES ${HDRS} )
source_group( "Generated Files\\Source" FILES ${moc_files} ${uiFiles} )
source_group( "UI Files" FILES ${UI_FILES} ${H_UI_FILES}  )


list( APPEND TSLIST "oracleSpatialAdapter_es" "oracleSpatialAdapter_pt" )

set (PRO_FILENAME ${ORACLESPATIALADAPTER_TVPLGS_DIR}/${PROJ_NAME}.pro )
foreach( TS ${TSLIST})
	set ( TSFILES "${TSFILES} ${ORACLESPATIALADAPTER_TVPLGS_DIR}/${TS}.ts" )
endforeach()

generatePRO( ${PRO_FILENAME} "${HEADERS}" "${SRCS}" "${UI_FILES}" "${TSFILES}" )

foreach( TS ${TSLIST})
	set ( QMFILE "${TRANSLATION_DIR_DEBUG}/${TS}.qm" )
	set ( TSFILE "${ORACLESPATIALADAPTER_TVPLGS_DIR}/${TS}.ts" )
	generateTS( ${TSFILE} ${PRO_FILENAME} )
	generateQM( ${TSFILE} ${QMFILE} )
	FILE ( COPY ${QMFILE} DESTINATION ${TRANSLATION_DIR_RELEASE} )
endforeach()

# -----------------------------------

add_definitions ( ${QT_DEFINITIONS} -DQT_THREAD_SUPPORT )

add_library ( ${PROJ_NAME} SHARED ${SRCS} ${HEADERS} ${HDRS} ${uiFiles} ${moc_files} ${H_UI_FILES}   ${UI_FILES})  

target_link_libraries ( ${PROJ_NAME} terralib terraViewCore )




