#Spatial Stat
# (c) André G de Oliveira, 2011
# ----------------------------------------------------------------------------
# A super project that constains all the progects related to the spatial Stat plugin

cmake_minimum_required(VERSION 2.8)

OPTION (BUILD_ATK "Build the ATK plugin?" ON)
OPTION (BUILD_BESAG "Build the Besag plugin?" ON)
OPTION (BUILD_BITHELL "Build the Bithell plugin?" ON)
OPTION (BUILD_BLIPPA "Build the BLiPPA plugin?" ON)
OPTION (BUILD_CUZICK "Build the Cuzick plugin?" ON)
OPTION (BUILD_DIGGLE "Build the Diggle plugin?" ON)
OPTION (BUILD_EBEBIN "Build the EBEbin plugin?" ON)
#Plugin não compila
#OPTION (BUILD_GRIMSON "Build the Grimson plugin?" ON)
#Plugin não compila
#OPTION (BUILD_IMORAN "Build the IMoran plugin?" ON)
OPTION (BUILD_IPOP "Build the Ipop plugin?" ON)
OPTION (BUILD_JACQUEZ_KNN "Build the Jacquez_kNN plugin?" ON)
#Plugin não compila
#OPTION (BUILD_KFUNCTIONS "Build the KFunctions plugin?" ON)
OPTION (BUILD_KNOX "Build the Knox plugin?" ON)
OPTION (BUILD_MANTEL "Build the Mantel plugin?" ON)
OPTION (BUILD_OPGAM "Build the Opgam plugin?" ON)
#Erro na hora de dar configure no cmake
#OPTION (BUILD_ROGERSON "Build the Rogerson plugin?" ON)
OPTION (BUILD_SCORE "Build the Score plugin?" ON)
OPTION (BUILD_SPATIALSCAN "Build the SpatialScan plugin?" ON)
#Erro na hora de dar configure no cmake
#OPTION (BUILD_SR "Build the SR plugin?" ON)
OPTION (BUILD_TANGO "Build the Tango plugin?" ON)

if(BUILD_ATK)
	add_subdirectory ( ATK )
endif()
if(BUILD_BESAG)
	add_subdirectory ( Besag )
endif()
if(BUILD_BITHELL)
	add_subdirectory ( Bithell )
endif()

#não funcionando em linux
#if(BUILD_BLIPPA)
#	add_subdirectory ( BLiPPA )
#endif()

if(BUILD_CUZICK)
	add_subdirectory ( Cuzick )
endif()
if(BUILD_DIGGLE)
	add_subdirectory ( Diggle )
endif()
if(BUILD_EBEBIN)
	add_subdirectory ( EBEbin )
endif()
#if(BUILD_GRIMSON)
#	add_subdirectory ( Grimson )
#endif()

#if(BUILD_IMORAN)
#	add_subdirectory ( IMoran )
#endif()
if(BUILD_IPOP)
	add_subdirectory ( Ipop )
endif()
if(BUILD_JACQUEZ_KNN)
	add_subdirectory ( Jacquez_kNN )
endif()
#if(BUILD_KFUNCTIONS)
#	add_subdirectory ( KFunctions )
#endif()
if(BUILD_KNOX)
	add_subdirectory ( Knox )
endif()
if(BUILD_MANTEL)
	add_subdirectory ( Mantel )
endif()
if(BUILD_OPGAM)
	add_subdirectory ( Opgam )
endif()
#if(BUILD_ROGERSON)
#	add_subdirectory ( Rogerson )
#endif()
if(BUILD_SCORE)
	add_subdirectory ( Score )
endif()
if(BUILD_SPATIALSCAN)
	add_subdirectory ( SpatialScan )
endif()
#if(BUILD_SR)
#	add_subdirectory ( SR )
#endif()
if(BUILD_TANGO)
	add_subdirectory ( Tango )
endif()

#Export variables
set ( BUILD_ATK ${BUILD_ATK} PARENT_SCOPE )
set ( BUILD_BESAG ${BUILD_BESAG} PARENT_SCOPE )
set ( BUILD_BITHELL ${BUILD_BITHELL} PARENT_SCOPE )
set ( BUILD_CUZICK ${BUILD_CUZICK} PARENT_SCOPE )
set ( BUILD_DIGGLE ${BUILD_DIGGLE} PARENT_SCOPE )
set ( BUILD_EBEBIN ${BUILD_EBEBIN} PARENT_SCOPE )
set ( BUILD_IPOP ${BUILD_IPOP} PARENT_SCOPE )
set ( BUILD_JACQUEZ_KNN ${BUILD_JACQUEZ_KNN} PARENT_SCOPE )
set ( BUILD_KNOX ${BUILD_KNOX} PARENT_SCOPE )
set ( BUILD_MANTEL ${BUILD_MANTEL} PARENT_SCOPE )
set ( BUILD_OPGAM ${BUILD_OPGAM} PARENT_SCOPE )
set ( BUILD_SCORE ${BUILD_SCORE} PARENT_SCOPE )
set ( BUILD_SPATIALSCAN ${BUILD_SPATIALSCAN} PARENT_SCOPE )
set ( BUILD_TANGO ${BUILD_TANGO} PARENT_SCOPE )